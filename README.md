# 网页版三国杀

标准身份场 4/5/8 人局,支持单机(1 名真人 + AI)与联机(真人不足由 AI 补足)。
游戏引擎与 UI 完全分离,单机与联机复用同一引擎与 AI。

## 运行

```bash
npm install
npm run dev      # 前端开发服务器(单机模式可直接玩)
npm run server   # 联机服务器(ws://localhost:8081,联机模式需要)
npm test         # 引擎单元测试 + 服务器集成测试(vitest)
npm run build    # 类型检查 + 生产构建
```

联机:一人在主菜单"创建联机房间"得到 4 位房间号,其他人凭房间号加入,
房主点开始;空位由 AI 补足,掉线凭 token 自动重连。

### 跨互联网部署(单端口)

`npm run server` 在检测到 `dist/` 时会**同端口托管游戏页面与 WebSocket**,
所以部署只需一台公网可达的机器:

```bash
# 方式一:直接跑(需 Node 20+)
npm ci && npm run build && npm run server   # 默认 8081,PORT=80 可换端口
# 云主机记得在安全组/防火墙放行该端口,然后所有人打开 http://服务器IP:8081

# 方式二:Docker(也适用于 Railway / Fly.io / Render 等容器平台)
docker build -t sgs . && docker run -d -p 8081:8081 sgs
```

页面与 WS 同源,客户端零配置。若要 HTTPS(页面走 https 时浏览器强制 wss),
在前面加一层反向代理即可,例如 Caddy 两行配置(自动签证书,WS 自动转发):

```
你的域名.com {
    reverse_proxy localhost:8081
}
```

前端若单独放静态托管(如 Pages)、服务器在别处,构建时用
`VITE_WS_URL=wss://服务器地址` 指定连接目标。

## 架构

```
src/
├── engine/        游戏引擎:纯 TS,零依赖,不接触 DOM/React
│   ├── types.ts   GameState / Action / PendingRequest / GameEvent 类型
│   ├── engine.ts  入口:createGame / applyAction(state, action) → { state, events }
│   ├── frames.ts  结算栈帧(杀-闪、伤害、濒死、判定、无懈链、决斗、锦囊)
│   ├── flow.ts    回合流程 + 出牌阶段处理(含全部主动技能)
│   ├── kernel.ts  区域移动 / 摸牌 / 死亡与胜负 等共享工具
│   ├── rules.ts   距离、攻击范围、出牌合法性
│   ├── setup.ts   建局(洗牌、分身份、分武将)
│   ├── rng.ts     可序列化随机数(状态存在 GameState.rngState 里)
│   └── view.ts    视角过滤 redactStateFor:隐藏他人手牌/身份/牌堆/随机数
├── ai/            simpleAi:读取状态 → 产出 ResponseData,绝不直接改状态
├── net/           protocol.ts:客户端↔服务器的 WebSocket 消息类型
├── server/        联机服务器:房间管理 + 权威对局(复用引擎与 AI),
│                  按玩家视角分发过滤后的状态;AI 座位/掉线/超时由服务器代答
├── game/          对局会话:LocalGame(本地宿主)与 NetGame(联机客户端),
│                  二者接口同构,UI 不感知状态来自哪里
└── ui/            React 单页应用(菜单 / 联机大厅 / 共用棋盘 GameBoard)
```

### 核心机制

- **applyAction 是纯函数**:非法 Action 返回 `error` 且状态原样;合法则返回新
  GameState + 本次事件列表。UI 动画与日志都消费事件。
- **GameState 完整可 JSON 序列化**:多步结算的全部进度记录在 `state.stack`
  的"结算栈帧"里(纯数据,无闭包),任意暂停点序列化后可原地继续 —— 这是
  联机同步与断线重连的基础。随机数状态也在 state 内,同 seed + 同 Action
  序列可完整重放。
- **请求-响应**:引擎任意时刻最多一个 `pendingRequest`,一切输入都是对它的
  应答(带 requestId 防过期/乱序)。人类由 UI 应答,AI 由 simpleAi 应答,
  联机时经 WebSocket 应答,接口完全一致。
- **联机安全模型**:服务器持有唯一权威状态;客户端只收到
  `redactStateFor(state, 玩家)` 的过滤副本 —— 他人手牌与牌堆替换为占位 id、
  未亮身份替换为占位值、随机数状态与结算栈不下发,改客户端也无法作弊偷看。
  断线凭 token 自动重连恢复座位;掉线/超时玩家由服务器代答,不会卡住对局。

## 已实现内容

- 身份场 4/5/8 人局(4人:主忠反内各1;5人:反×2;8人:忠×2 反×4),
  主公 +1 体力上限,死亡奖惩、胜负判定
- **开局选将**:主公先选(候选数 +2),其余角色按座次依次选;
  他人候选与选择过程按视角过滤,联机不可偷看
- **对局设置**(持久化,单机与联机建房均生效):自选/随机武将、
  候选数 3~5 选 1(8 人局按武将池自动下调)、AI 出牌延迟快/标准/慢、
  神将入池开关、头像风格(内置卡通插画 / 经典卡牌头像)
- **标准包 25 将全部实现**:
  - 魏:曹操(奸雄)、司马懿(反馈/鬼才)、夏侯惇(刚烈)、张辽(突袭)、
    许褚(裸衣)、郭嘉(天妒/遗计)、甄姬(洛神/倾国)
  - 蜀:刘备(仁德)、关羽(武圣)、张飞(咆哮)、诸葛亮(观星/空城)、
    赵云(龙胆)、马超(马术/铁骑)、黄月英(集智/奇才)
  - 吴:孙权(制衡/救援)、甘宁(奇袭)、吕蒙(克己)、黄盖(苦肉)、
    周瑜(英姿/反间)、大乔(国色/流离)、陆逊(谦逊/连营)、
    孙尚香(结姻/枭姬)
  - 群:华佗(急救/青囊)、貂蝉(离间/闭月)、吕布(无双)
- **风包 8 将全部实现**:
  - 夏侯渊(神速)、曹仁(据守,含武将翻面)、黄忠(烈弓)、魏延(狂骨)、
    小乔(天香/红颜)、周泰(不屈)、张角(雷击/鬼道)、于吉(蛊惑)
- **火包 8 将全部实现**(引入拼点机制与限定技):
  - 典韦(强袭)、荀彧(驱虎/节命)、庞统(连环/涅槃)、
    诸葛亮·卧龙(八阵/火计/看破)、太史慈(天义)、庞德(马术/猛进)、
    颜良文丑(双雄)、袁绍(乱击/血裔,主公技手牌上限)
- **神武将 3 将**(设置开关,默认关;登场时自选势力,不进主公候选):
  - 神关羽(武神/武魂)、神吕蒙(涉猎/攻心)、神曹操(归心/飞影)
- **一将成名 2011 全部 11 将实现**:
  - 魏:曹植(落英/酒诗)、张春华(绝情/伤逝)、于禁(毅重)
  - 蜀:法正(恩怨/眩惑)、马谡(心战/挥泪)、徐庶(无言/举荐)
  - 吴:凌统(旋风)、徐盛(破军)、吴国太(甘露/补益)
  - 群:陈宫(明策/智迟)、高顺(陷阵/禁酒)
- **林包 8 将全部实现**:
  - 曹丕(行殇/放逐/颂威)、徐晃(断粮)、孟获(祸首/再起)、
    祝融(巨象/烈刃)、鲁肃(好施/缔盟)、孙坚(英魂)、
    董卓(酒池/肉林/崩坏/暴虐,8 体力)、贾诩(完杀/乱武/帷幕)
- **标准版 + 军争篇牌堆全部实现**(138 张):
  - 基本牌:杀、火杀、雷杀(属性伤害)、闪、桃、酒(杀+1 伤/濒死自救)
  - 锦囊:过河拆桥、顺手牵羊、无中生有、决斗、无懈可击(支持无懈反制无懈)、
    南蛮入侵、万箭齐发、五谷丰登、桃园结义、借刀杀人、火攻、
    铁索连环(横置传导/重铸)(均逐目标可无懈)
  - 延时锦囊:乐不思蜀、闪电(雷伤 3 点,不中沿座次轮转)、兵粮寸断
  - 武器:诸葛连弩、青龙偃月刀、雌雄双股剑、寒冰剑、丈八蛇矛、贯石斧、
    方天画戟(最后手牌杀至多三目标)、麒麟弓、朱雀羽扇(普通杀当火杀)、
    古锭刀(目标无手牌伤害+1)
  - 防具:八卦阵、仁王盾(黑杀无效)、藤甲(普通杀/南蛮/万箭无效,火伤+1)、
    白银狮子(伤害上限 1,失去时回复);±1马
  - **属性伤害与连环**:火/雷伤害对连环角色解除横置并传导给其他连环角色
- 完整回合流程(含判定阶段延时锦囊结算)、距离计算(含马/马术)、
  濒死求桃、牌堆耗尽重洗
- UI:选将弹窗(头像/势力/体力/技能一览)、手牌点选、目标点击、技能按钮、
  响应弹窗 + 20 秒倒计时(超时默认应答)、
  出牌历史日志、座位号标注、中央出牌展示板(动画)、
  按钮文案随动作变化(重铸/发动技能名)、AI 出牌间隔可观察、
  指向性卡牌/技能出牌时座位间的指向箭头动画
- 武将头像:内置全套自绘矢量插画(按势力配色描边);设置切到"经典卡牌"
  则优先使用 `public/generals/official/<武将id>.png`(项目所有者自备的
  卡牌头像,版权归游卡桌游,仅个人使用,见该目录 SOURCES.md;缺图自动
  回退卡通);`npm run portraits` 可从 Wikimedia Commons 下载公有领域
  历史画像作为第三种选择,或手动放入 `public/generals/<武将id>.jpg`

## 已知简化(后续迭代)

- AI 敌我判断直接读取身份(不看他人手牌);后续改为基于 `viewFor` 视角 + 身份推理
- 遗计一次分配只能交给一名角色(可多轮分配给不同人)
- 蛊惑用简化裁定:依次询问其他角色是否质疑,质疑真牌者失去 1 点体力
  (界蛊惑改为获得缠怨),假牌被质疑则作废;暂只支持出牌阶段声明
- 拼点为顺序选牌(先发起者后目标),非同时暗选;AI 拼点固定出最大点数
- 行殇/颂威/落英/伤逝/酒诗翻回/新生简化为自动发动
- 无言简化为"不能使用/不能被指定"非延时锦囊;心战简化为自动收红桃、
  其余不重排;智迟覆盖杀/普通锦囊/AOE/火攻/铁索
- 天义按 2009 原版简化:赢=杀无距离限制且次数 +1,输=本回合不能使用杀

已完善的历史简化:主公技全部实现(救援/血裔/颂威/暴虐/激将/护驾,
激将与护驾支持响应代打与出牌阶段主动发动);
选将主公候选保证包含经典主公将(曹/刘/孙,含界版);
武圣/急救/倾国支持装备牌转化;八卦阵/八阵可响应万箭齐发;
酒池可在濒死时将黑桃当酒自救;鬼才/鬼道 AI 会为队友改判(乐/兵粮/闪电/雷击);
遗计 AI 会把废牌分给受伤队友。

## 武将扩展路线

按步骤逐包扩充(当前进度:✅ 标准包 + 风火林山 + 一将成名2011 + 神将×3 + 界限突破×33,共 104 将):

1. ✅ 标准包(25 将 + 全部标准版牌)
2. ✅ 军争篇牌(属性伤害/连环/酒/火攻/兵粮/军争装备)
3. ✅ 风包(夏侯渊、曹仁、黄忠、魏延、小乔、周泰、张角、于吉)
4. ✅ 火包(典韦、荀彧、庞统、卧龙诸葛、太史慈、庞德、颜良文丑、袁绍)
5. ✅ 林包(曹丕、徐晃、孟获、祝融、鲁肃、孙坚、董卓、贾诩)
6. ✅ 山包(邓艾、张郃、姜维、刘禅、孙策、张昭张纮、左慈、蔡文姬)
7. ✅ 一将成名 2011(曹植、张春华、于禁、法正、马谡、徐庶、凌统、徐盛、吴国太、陈宫、高顺)
8. ◐ 风火林神将:✅ 神关羽、神吕蒙、神曹操;⬜ 神周瑜、神诸葛亮、神吕布
9. ✅ 标准/风包 界限突破(33 将,独立武将与原版共存)

新增武将的套路:在 `generals.ts` 注册 → 技能按类型落位
(主动技进 `flow.ts` 的 useSkill;转化技进 `rules.ts` 的 validateResponseCard;
触发技进对应结算帧或 `kernel.ts` 钩子)→ `portraits.tsx` 加头像配置 →
`standard.test.ts` 式的用例覆盖。
